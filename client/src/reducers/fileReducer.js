//дефолтное состояние редьюсера
const defaultState = {

}

//первый параметр - состояние, второй - action
export default function fileReducer(state = defaultState, action){	//сам редьюсер
	switch (action.type) {
		
	
		default:
			return state;
	}
}